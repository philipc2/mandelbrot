[project]
name = "py-mandelbrot"
version = "0.1.0"
description = "Python package to visualuze the Mandelbrot Set generated by a Node-JS Package"
requires-python = ">=3.8"
authors = [
    { name = "Philip Chmielowiec", email = "chmielowiec.philip@gmail.com" }
]
dependencies = [
    "click",
    "matplotlib",
    "colorcet",
    "pillow"
]

[project.scripts]
mandelbrot-vis = "mandelbrot.cli:main"
